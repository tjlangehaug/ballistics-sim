# Handoff: plan-phase-0 — 2026-09-19
Role: arch   Branch: arch/plan-phase-0   PR: (opening now, split in two)

## What changed, and why
- `docs/decisions/0007-fixture-file-format.md` (Accepted): published
  fixtures are one TOML file each, per ADR (metadata + `[[range]]`
  array-of-tables). Chosen over CSV+sidecar and RON — see ADR for the
  full case.
- `docs/decisions/0008-earth-frame-coriolis-scope.md` (Accepted, human
  decision 2026-09-19): Earth-frame Coriolis/Eötvös deferred to Phase 1.
  "Leave Coriolis effect for a future phase. I want a good basic physics
  model at this point." Phase 0's point-mass model is gravity + drag
  only; no firing-point latitude/azimuth input this phase.
- `docs/plan/phase-0-validation.md` rewritten from draft into a
  sequenced task list (7 tasks, one already done: `pr-gates`) with a
  dependency graph, and the HUMAN DECISIONS section trimmed to the two
  still open (fixture source, tolerances — both deferred to whoever
  executes `validation-harness`, for human approval in that PR).
- `docs/plan/00-roadmap.md`: ticked the two Setup checklist items this
  session completes ("First PR through the loop", "Architect session:
  Phase 0 decomposed").
- Seven task briefs added in `docs/briefs/` (this PR's sibling —
  see below), one per unchecked Phase 0 task.
- Why: Setup checklist item "Architect session: Phase 0 decomposed into
  sub-400-line briefs" (`docs/plan/00-roadmap.md`), per
  `docs/agents/architect.md`.

## Deliberately NOT done
- Did not decide the published fixture source(s) or per-quantity
  tolerances — both are physics/data judgment calls left for the
  `validation-harness` task's executor to propose and the human to
  approve in that PR, per `docs/agents/test.md`.
- Split this work into two PRs on request: this PR carries the ADRs +
  plan; a second PR (branch `arch/plan-phase-0-briefs`, based on this
  one) carries the seven task briefs. Both together are this session's
  full output — see that PR's own handoff note for briefs specifics.

## Surprises a future session must know
- Found a real contradiction between `docs/agents/test.md`'s general
  guidance (write tests ahead of the feature, marked
  `#[ignore = "awaits <feature>"]`) and `gate-test-integrity`, which
  fails on *any* added `#[ignore]` with no exception for that
  convention. Not resolved here — flagged in
  `docs/plan/phase-0-validation.md`'s Sequencing section for a future
  docs close-out to reconcile. Phase 0's own task order sidesteps it
  (`validation-harness` is sequenced after `point-mass-rk4`, so its
  test calls a solver that already exists and needs no `#[ignore]`).
- G1+G7 standard drag tables were split into two separate tasks
  (`drag-model-core`, `drag-model-g7`) instead of the draft's single
  `drag-tables` task — the combined data tables risked exceeding the
  400-line budget by themselves.

## Exact next action
1. Review and merge this PR, then the `arch/plan-phase-0-briefs` PR.
2. Human decisions still open before `validation-harness` can start:
   published fixture source(s) and per-quantity tolerances (see
   `docs/plan/phase-0-validation.md` HUMAN DECISIONS section).
3. First worker task ready to hand out once merged: `units-atmosphere`
   (`just task units-atmosphere ballistics`) — no blockers.
